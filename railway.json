{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m playwright install chromium && streamlit run app.py --server.port=$PORT --server.address=0.0.0.0"
  }
}
